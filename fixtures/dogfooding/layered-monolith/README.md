# Layered Monolith Fixture

This fixture simulates a repository that already follows a layered modular monolith.

## Delivery Slice

Add filterable audit-log search without changing the repository's current module structure.

## Existing Structure

- `src/modules/audit-log/api`
- `src/modules/audit-log/service`
- `src/modules/audit-log/repo`

The expected default is `existing-repo-pattern` or `layered`.
