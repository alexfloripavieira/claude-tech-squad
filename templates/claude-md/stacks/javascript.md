## Stack Notes

- Use the package manager already present in lockfiles.
- Prefer script entries from `package.json` over direct tool invocation.
- Keep runtime, build, and test changes separated when possible.

## Default Commands

```bash
npm run dev
npm test
npm run lint
npm run build
```
