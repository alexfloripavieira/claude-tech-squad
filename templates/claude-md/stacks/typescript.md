## Stack Notes

- Keep exported types and runtime behavior in sync.
- Prefer existing test and lint scripts from `package.json`.
- Run type checks before finalizing broad API changes.

## Default Commands

```bash
npm run dev
npm test
npm run lint
npm run build
```
